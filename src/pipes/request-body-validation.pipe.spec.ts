import { RequestBodyValidationPipe } from './request-body-validation.pipe';

describe('RequestBodyValidationPipe', () => {
  it('should be defined', () => {
    expect(new RequestBodyValidationPipe()).toBeDefined();
  });
});
